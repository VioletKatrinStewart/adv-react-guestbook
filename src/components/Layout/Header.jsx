import React from 'react';
// import { useUser } from '../../context/UserContext';

export default function Header() {
  //   const { user } = useUser();
  return <div>Hey! This is the header</div>;

  //   <div>Hi {user ? 'user' : { user }}</div>;
}
